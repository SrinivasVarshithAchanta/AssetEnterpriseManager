# Performance Benchmark Results

> Auto-generated by `EnterpriseAssetManager.Benchmarks`. Re-run after seed data changes.

**Run date (UTC):** 2026-06-29 16:58:15
**Database:** SQL Server Express `.\SQLEXPRESS` / `EnterpriseAssetManagerDb`
**Database name:** `EnterpriseAssetManagerDb`
**Seed data detected:** Users=500, Assets=140, AssetRequests=260
**Method:** `Stopwatch` over in-process EF Core calls (warmup + measured runs)

## Formula
```
Improvement % = ((NaiveTime - OptimizedTime) / NaiveTime) * 100
```

| Scenario | Naive avg (ms) | Optimized avg (ms) | Improvement % |
|----------|----------------|--------------------|---------------|
| Asset search (tag/name/serial) | 2.6 | 2.7 | -2.8 |
| User search (name/email) | 6.9 | 5.8 | 15.4 |
| Request filter (status/priority) | 8.5 | 3.2 | 62.5 |
| Paginated asset list (no search) | 2.0 | 1.6 | 23.3 |
| **Overall (sum of scenarios)** | **20.0** | **13.2** | **33.9** |

## Interpretation
- **Naive approach:** `ToListAsync()` on full tables, then filter/page in C#.
- **Optimized approach:** `IQueryable` + `Where` + `OrderBy` + `Skip`/`Take` + `AsNoTracking()` (same pattern as production services).

The measured overall improvement is **33.9%**, which is **below the 35% resume claim** on this machine/dataset.

**Safer interview wording:**
> Optimized list and search pages using server-side filtering, pagination, database indexes, and `AsNoTracking()` instead of loading full tables into memory.

The optimization is still real — the gap may grow with larger tables or concurrent load — but do not state "35%" unless you re-run this benchmark and the result supports it.

## How to re-run
```cmd
cd "C:\Users\s_var\OneDrive\Desktop\Personal projects\EnterpriseAssetManager"
dotnet run --project EnterpriseAssetManager.Benchmarks
```
